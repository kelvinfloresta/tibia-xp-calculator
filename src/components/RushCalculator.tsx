import React from 'react';
import Title from 'antd/es/typography/Title';

export function RushCalculator() {
	return (
		<Title
			level={2}
			style={{
				width: '100%',
				textAlign: 'center',
				lineHeight: 10,
			}}
		>
			Em breve
		</Title>
	);
}
